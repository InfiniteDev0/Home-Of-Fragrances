"use client";

import React from "react";
// import { countryRegions } from "@/lib/countries";
// import { useRouter } from "next/navigation";
// import { saveUserLocation } from "@/lib/locationUtils";

export default function CountrySelector() {
  // const router = useRouter();

  // const handleCountrySelect = (country) => {
  //   // Save country selection
  //   saveUserLocation(country);

  //   // Redirect to country-specific homepage
  //   const url = `/${country.lan.toLowerCase()}-${country.code.toLowerCase()}/homepage?dispatchCountry=${
  //     country.code
  //   }`;
  //   router.push(url);
  // };

  return (
    // <div className="min-h-screen bg-white">
    //   <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    //     <h1 className="text-3xl font-bold text-center mb-8">
    //       Choose Your Country/Region
    //     </h1>

    //     <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-8">
    //       {countryRegions.map((region) => (
    //         <div key={region.name} className="space-y-4">
    //           <h2 className="text-xl font-semibold">{region.name}</h2>
    //           <div className="space-y-2">
    //             {region.countries.map((country) => (
    //               <button
    //                 key={country.code}
    //                 onClick={() => handleCountrySelect(country)}
    //                 className="w-full flex items-center justify-between p-3 hover:bg-gray-50 rounded-lg transition-colors text-left"
    //               >
    //                 <div className="flex items-center space-x-3">
    //                   <span className="text-lg">{country.name}</span>
    //                 </div>
    //                 <div className="flex items-center space-x-2 text-gray-500">
    //                   <span>{country.currency}</span>
    //                   <span className="text-sm">
    //                     ({country.lan.toUpperCase()})
    //                   </span>
    //                 </div>
    //               </button>
    //             ))}
    //           </div>
    //         </div>
    //       ))}
    //     </div>
    //   </div>
    // </div>
    <div></div>
  );
}
